<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Cambiamos a NoActionBar para que no nos muestre el menu por defecto. La barra de accion de la perte de arriba -->
    <style name="Theme.ProdecoConf" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- Desactivamos la pantalla completa. -->
        <item name="android:windowFullscreen">false</item>
        <!-- Ocultamos el titulo por defecto del Toolbar. -->
        <item name="android:windowNoTitle">true</item>

    </style>
</resources>