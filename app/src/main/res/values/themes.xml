<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Cambiamos a NoActionBar para que no nos muestre el menu por defecto. La barra de accion de la perte de arriba -->
    <style name="Theme.ProdecoConf" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <!-- Desactivamos la pantalla completa. -->
        <item name="android:windowFullscreen">false</item>
        <!-- Ocultamos el titulo por defecto del Toolbar. -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Estilo Personalizado para el detalle de los docuemntos. -->
    <style name="FullScreenDialogStyle" parent="Theme.MaterialComponents.DayNight.Dialog">
        <!-- Ocultamos el titulo por defecto del Dialog. -->
        <item name="android:windowNoTitle">true</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <!-- Desactivamos la pantalla completa. -->
        <item name="android:windowFullscreen">false</item>
        <!-- No permita flotar sobre la pantalla -->
        <item name="android:windowIsFloating">false</item>
        <!-- Color de fondo a los detalles -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- ANIMACIONES. -->
        <!-- Como se abre y se cierra la animacion. -->
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>

    </style>
</resources>